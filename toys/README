capturecard: convert capture card to h264/hevc/vp9 opus (low latency) in 5 Mbps or other bitrate efficiently. you need intel processor with qsv to run it as it uses a lot of optimizations feature from that

playcapturecard: show the capture card's capture with low latency audio & video. you need intel processor with qsv. uses mpv
